/**
 * OWNER: Track 2 (Backend / Agents).
 *
 * Implementar acá:
 *   - runStrategy({ projectId, runId }): Promise<StrategyOutput>
 *   - tools: get_products(project_id), get_brand_brief(project_id)
 *   - debe usar publishEvent() del bus en cada paso.
 *
 * Si MOCK_STRATEGY=true, devolver MOCK_STRATEGY_OUTPUT de @/lib/mocks/strategy.
 *
 * Validar output con StrategyOutputSchema antes de persistir.
 */
export {};
